# Data

Mock data has been removed. All data is now fetched from Supabase.

- Users        → `public.profiles` (auto-created on sign-up)
- Projects     → `public.projects`
- Tasks        → `public.tasks`
- Technologies → `public.technologies` (seeded via schema.sql)
- Members      → `public.project_members`
